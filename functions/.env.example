# Example only. Do not commit real secrets to the repository.
# Real Gemini keys must stay backend-only and should never be exposed to the frontend.
# For deployed Firebase Functions later, use Firebase secrets or secure backend env configuration.
GEMINI_API_KEY_LOCAL=your_backend_only_gemini_key_here
GEMINI_MODEL=gemini-1.5-flash
